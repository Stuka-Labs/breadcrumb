import 'package:flutter/material.dart';

class UsersCVScreen extends StatelessWidget {
  const UsersCVScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Users (CV)')),
      body: const Center(child: Text('Users (CV) Page')),
    );
  }
} 