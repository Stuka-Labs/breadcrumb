import 'package:flutter/material.dart';

class ParseAFileScreen extends StatelessWidget {
  const ParseAFileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parse a File')),
      body: const Center(child: Text('Parse a File Page')),
    );
  }
} 