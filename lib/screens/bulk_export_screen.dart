import 'package:flutter/material.dart';

class BulkExportScreen extends StatelessWidget {
  const BulkExportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Export')),
      body: const Center(child: Text('Bulk Export Page')),
    );
  }
} 