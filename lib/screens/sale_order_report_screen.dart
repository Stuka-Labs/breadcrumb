import 'package:flutter/material.dart';

class SaleOrderReportScreen extends StatelessWidget {
  const SaleOrderReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sale Order (CV) Report')),
      body: const Center(child: Text('Sale Order (CV) Report Page')),
    );
  }
} 