import 'package:flutter/material.dart';

class RateCardsScreen extends StatelessWidget {
  const RateCardsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rate Cards')),
      body: const Center(child: Text('Rate Cards Page')),
    );
  }
} 