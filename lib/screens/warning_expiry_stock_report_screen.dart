import 'package:flutter/material.dart';

class WarningExpiryStockReportScreen extends StatelessWidget {
  const WarningExpiryStockReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Warning Expiry Stock Report (CV)')),
      body: const Center(child: Text('Warning Expiry Stock Report (CV) Page')),
    );
  }
} 