import 'package:flutter/material.dart';

class BulkExportPurchaseScreen extends StatelessWidget {
  const BulkExportPurchaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Export Purchase')),
      body: const Center(child: Text('Bulk Export Purchase Page')),
    );
  }
} 