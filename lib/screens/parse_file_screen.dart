import 'package:flutter/material.dart';

class ParseFileScreen extends StatelessWidget {
  const ParseFileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parse File (CV)')),
      body: const Center(child: Text('Parse File (CV) Page')),
    );
  }
} 