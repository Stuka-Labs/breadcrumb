import 'package:flutter/material.dart';

class ApiClientUsageScreen extends StatelessWidget {
  const ApiClientUsageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('API Client Usage')),
      body: const Center(child: Text('API Client Usage Page')),
    );
  }
} 