import 'package:flutter/material.dart';

class DiscrepancyReportsScreen extends StatelessWidget {
  const DiscrepancyReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Discrepancy Reports')),
      body: const Center(child: Text('Discrepancy Reports Page')),
    );
  }
} 