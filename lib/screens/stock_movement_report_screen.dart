import 'package:flutter/material.dart';

class StockMovementReportScreen extends StatelessWidget {
  const StockMovementReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Stock Movement (PV) (CV) Report')),
      body: const Center(child: Text('Stock Movement (PV) (CV) Report Page')),
    );
  }
} 