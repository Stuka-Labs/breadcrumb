import 'package:flutter/material.dart';

class PurchaseOrderReportScreen extends StatelessWidget {
  const PurchaseOrderReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Purchase Order (CV) Report')),
      body: const Center(child: Text('Purchase Order (CV) Report Page')),
    );
  }
} 