import 'package:flutter/material.dart';

class PrintLogScreen extends StatelessWidget {
  const PrintLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Print Log (PV)')),
      body: const Center(child: Text('Print Log (PV) Page')),
    );
  }
} 