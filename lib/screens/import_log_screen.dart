import 'package:flutter/material.dart';

class ImportLogScreen extends StatelessWidget {
  const ImportLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Import Log')),
      body: const Center(child: Text('Import Log Page')),
    );
  }
} 