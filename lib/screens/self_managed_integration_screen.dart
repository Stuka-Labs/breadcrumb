import 'package:flutter/material.dart';

class SelfManagedIntegrationScreen extends StatelessWidget {
  const SelfManagedIntegrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Self Managed Integration (CV)')),
      body: const Center(child: Text('Self Managed Integration (CV) Page')),
    );
  }
} 