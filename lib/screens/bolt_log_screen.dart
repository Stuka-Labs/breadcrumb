import 'package:flutter/material.dart';

class BoltLogScreen extends StatelessWidget {
  const BoltLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bolt Log')),
      body: const Center(child: Text('Bolt Log Page')),
    );
  }
} 