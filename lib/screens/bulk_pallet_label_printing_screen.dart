import 'package:flutter/material.dart';

class BulkPalletLabelPrintingScreen extends StatelessWidget {
  const BulkPalletLabelPrintingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Pallet Label Printing (PV)')),
      body: const Center(child: Text('Bulk Pallet Label Printing (PV) Page')),
    );
  }
} 