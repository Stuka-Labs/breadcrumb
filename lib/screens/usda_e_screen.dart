import 'package:flutter/material.dart';

class UsdaEScreen extends StatelessWidget {
  const UsdaEScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Usda e')),
      body: const Center(child: Text('Usda e Page')),
    );
  }
} 