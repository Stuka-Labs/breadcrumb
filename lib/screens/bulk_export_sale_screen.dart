import 'package:flutter/material.dart';

class BulkExportSaleScreen extends StatelessWidget {
  const BulkExportSaleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Export Sale')),
      body: const Center(child: Text('Bulk Export Sale Page')),
    );
  }
} 